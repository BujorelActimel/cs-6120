@main {
    a: int = const 3;
    b: int = add a a;
    cond: bool = lt a b;
    br cond .always .never;
    .always:
        print a;
        jmp .end;
    .never:
        print b;
        jmp .end;
    .end:
}
